// 1.
const arrowMyFunction = (num1, num2) => {
    return `${num1} + ${num2}`
}
// 1. stops here



//2.

const arrowAdd100 = (add100) => money + 100

//2. stops here


//3.
const letters = (message, name, p) => {
    return `${message} ${name} ${p}`
}
console.log(letters("hello my name is", "pete", "p"));

//3. stops here


